---

- name: Create users
  hosts: slave1
  tasks:
      - name: Create users 
      - include_vars: /home/ubuntu/ansible/data/users.yml
        user: 
            name: "{{ item }}"
            state: present
        with_items: "{{ admins }}"
      
    
    

